"""
Settings views — Users + Tokens live here. Cluster + namespace management
moved to core/views/clusters.py (first-class top-nav section).

This file is retained for potential future Access-scoped settings; the
user/token views keep their own modules (core/views/users.py, tokens.py).
"""
